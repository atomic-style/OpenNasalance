# atomic_gpio

'atomic_gpio' is a component to:
    - initialize, write, read & gpio levels.

## To add to project:

- Add "atomic_gpio" to CMakeLists.txt REQUIRES or PRIV_REQUIRES
- Add "#include atomic_gpio.h" to source files

