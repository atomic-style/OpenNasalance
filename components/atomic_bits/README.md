# atomic_bits

'atomic_bits' is a component to:
    - initialize, write, read & debug EventGroup bits.

## To add to project:

- Add "atomic_bits" to CMakeLists.txt REQUIRES or PRIV_REQUIRES
- Add "#include atomic_bits.h" to source files
- Call "atomic_bits_init()" to mount card

