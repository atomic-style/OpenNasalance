# atomic_info

'atomic_info' is a minimal component which logs esp32 hardware information.

## To add to project:

- Add "atomic_info" to CMakeLists.txt REQUIRES or PRIV_REQUIRES
- Add "#include atomic_info.h" to source files
- Call "atomic_info_print();"

